To build and test the application:
1. Open a terminal window.
2. Navigate to the root directory of the project.
3. Run the following command: `make build`

To create a deployable package or executable:
1. Open a terminal window.
2. Navigate to the root directory of the project.
3. Run the following command: `make package`